# 3.2: GitHub 데이터 수집 클라이언트

**Story ID:** 3.2
**Epic:** 3 — Repo 분석 (커밋/PR 포함)
**Slug:** github-data-collector
**Status:** done
**Branch:** `feature/3.2-github-data-collector` (base: `feature/nextjs-migration`)

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **개발자(본인, 파이프라인 구현자 관점)**,
I want **README/디렉토리 구조/커밋/PR을 GitHub API로 수집하는 클라이언트 모듈을 만들**,
so that **분석 Job 파이프라인(3.3)이 이 모듈을 호출해 노트 생성에 필요한 데이터를 얻을 수 있다**.

## Acceptance Criteria

1. `lib/domain/analysis/github-client.ts`에 README와 최상위 디렉토리 구조를 가져오는 함수가
   있다.
2. 지정 브랜치(또는 default branch)의 최신 커밋 최대 50개를 가져오는 함수가 있다.
3. 지정 브랜치 관련 PR 최대 20개(제목/설명/상태)를 가져오는 함수가 있다.
4. README가 없거나 PR이 없는 repo에서도 함수가 예외를 던지지 않고 빈 값/구조로 처리한다.
5. GitHub API 호출 실패(네트워크 오류, 404 등) 시 명확한 에러가 throw되어 호출자가 원인을
   구분할 수 있다.

## Tasks / Subtasks

- [x] Octokit 클라이언트 초기화 (AC: #1, #2, #3)
- [x] README/디렉토리 구조 조회 함수 (AC: #1, #4)
- [x] default branch 조회 + 커밋 목록(최신 50개) 조회 함수 (AC: #2, #4)
- [x] PR 목록(최근 20개) 조회 함수 (AC: #3, #4)
- [x] 에러 타입 정의 및 처리 (AC: #5)

## Dev Notes

- 커밋 최신 50개 / PR 최근 20개 상한은 ADR-009의 LOCKED 결정이다 — GitHub API 클라이언트는
  반드시 이 상한을 페이지네이션 파라미터로 강제해야 한다.
  [Source: architecture.md#adr-009-github-커밋pr-수집-범위-제한-및-캐싱]
- branch가 지정되지 않으면 GitHub API로 repo의 default branch를 조회해 사용한다.
  [Source: architecture.md#adr-009-github-커밋pr-수집-범위-제한-및-캐싱]
- 이 스토리는 순수 함수/클라이언트 모듈만 만든다. Job 파이프라인 연동(호출 지점)은 3.3에서
  수행한다. [Inference]
- GitHub API 호출은 사용자의 저장된(복호화된) `access_token`을 사용한다 — 1.3에서 만든 암호화
  유틸의 복호화 함수를 재사용한다. [Source: architecture.md#adr-003-github-oauth-via-authjs-jwt-세션]

## Testing

- **Approach:** 단위 테스트(Octokit mock).
- **Key scenarios:** README 있음/없음, 커밋 50개 초과 repo에서 상한 적용, PR 없는 repo, API 실패.
- **Per-AC verification:** AC#1 → mock으로 README 반환 확인; AC#2 → 커밋 50개 초과 fixture로
  상한 적용 확인; AC#3 → PR 20개 초과 fixture로 상한 적용 확인; AC#4 → README/PR 없는 fixture로
  빈 값 처리 확인; AC#5 → mock 404/500 응답 시 특정 에러 타입 throw 확인.
- **Fixtures / mocks:** Octokit mock 응답(README 있음/없음, 커밋 다수, PR 다수/0개, API 에러
  케이스).
- **Edge cases to cover:** private repo 접근 권한 없음(403), GitHub API rate limit 응답
  (403/429).

## Dependency Maps

- **Blocked by:** 1.3 (`access_token` 복호화 유틸)
- **Blocks:** 3.3
- **External:** GitHub REST API, Octokit

## Owned File/Module Scope

- `lib/domain/analysis/github-client.ts`
- `tests/domain/analysis/github-client.test.ts`

## Learnings from Previous Stories

- 1.3의 암호화/복호화 유틸을 재사용한다 (from 1.3).

## Dev Agent Record

**완료일:** 2026-08-03 (Ralph loop)
**Branch:** `feature/3.2-github-data-collector` (base: `feature/nextjs-migration`)

**진행 요약:**
- `octokit` 패키지 설치. `lib/domain/analysis/github-client.ts`에 4개 함수:
  `getDefaultBranch`, `getRepoOverview`(README+최상위 디렉토리), `getRecentCommits`(최대 50),
  `getRecentPullRequests`(최대 20, `base: branch`로 필터).
- 모든 함수는 `encryptedAccessToken`을 받아 함수 내부에서 1.3의 `decrypt()`로 복호화한 뒤
  Octokit 인스턴스를 생성 — 토큰을 평문으로 오래 들고 있지 않도록 매 호출마다 새로
  복호화/인스턴스화.
- README/디렉토리 조회는 404를 정상 케이스(빈 값)로 처리하고, 그 외 상태 코드는
  `GithubApiError`(status 보존)로 throw.
- `getRecentCommits`/`getRecentPullRequests`는 GitHub API의 `per_page` 파라미터로 상한을
  요청하지만, 방어적으로 응답 배열도 `.slice(0, MAX)`로 한 번 더 자름 (API가 상한을 넘겨줄
  가능성에 대비).
- 단위 테스트 9건: Octokit을 `vi.mock("octokit")`으로 모킹. **한 가지 함정 발견** — 모킹 시
  `vi.fn().mockImplementation(() => ({...}))`처럼 화살표 함수를 쓰면 `new Octokit()`이
  "is not a constructor" 에러를 낸다 (화살표 함수는 `[[Construct]]`가 없어서). 반드시
  `function MockOctokit() { return {...}; }` 같은 일반 함수 표현식을 써야 한다. [Learning]
  전체 스위트 56건 통과. `npm run build`/`lint` 통과.
- 이 스토리는 순수 클라이언트 모듈이라 실제 GitHub API 호출 없이 전부 mock으로 검증
  가능했음 — "실제 자격증명 필요" 항목 없음. 실제 GitHub API와의 통합 동작은 3.3(파이프라인)
  구현 시 실제 access_token으로 한 번 확인해보면 좋음.

**후속 스토리를 위한 참고:**
- 3.3이 이 4개 함수를 호출해 파이프라인을 구성한다. `encryptedAccessToken`은
  `users.github_token` 컬럼 값을 그대로 넘기면 된다.
