# 4.2: 노트 목록/상세 조회 API + 화면

**Story ID:** 4.2
**Epic:** 4 — 학습 노트
**Slug:** note-list-detail-api-ui
**Status:** ready-for-dev
**Branch:** `feature/4.2-note-list-detail-api-ui` (base: `develop`)

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **사용자(본인)**,
I want **생성된 학습 노트의 목록과 상세 내용을 조회하**,
so that **과거에 학습한 내용을 다시 찾아보고 복습할 수 있다**.

## Acceptance Criteria

1. `GET /api/notes`는 본인 소유 노트 목록(title, summary, createdAt)을 페이지네이션하여
   반환한다.
2. `GET /api/notes/{noteId}`는 노트 상세(summary, concepts, architecture, learningPoints,
   rawMarkdown)를 반환한다.
3. 타인 소유 노트 상세 조회 시 403, 존재하지 않는 노트는 404가 반환된다.
4. `/notes` 목록 페이지와 `/notes/{noteId}` 상세 페이지가 각각 존재한다.

## Tasks / Subtasks

- [ ] `GET /api/notes` Route Handler (AC: #1)
- [ ] `GET /api/notes/{noteId}` Route Handler + 소유권 검증 (AC: #2, #3)
- [ ] `/notes` 목록 페이지 UI (AC: #4)
- [ ] `/notes/{noteId}` 상세 페이지 UI (AC: #4)

## Dev Notes

- FR-014/FR-015는 Must 우선순위 요구사항이다.
  [Source: prd.md#fr-014-노트-목록-조회--must]
- 4.1의 `note-service.ts`에 조회 함수(`listNotes`, `getNoteById`)를 추가하는 방식으로 확장한다.
  [Inference]
- 응답 형식은 ADR-005 엔벨로프, 소유권 검증은 1.4의 `requireUser()` 패턴을 재사용한다.
  [Source: architecture.md#adr-005-rest--json-응답-엔벨로프-규약]

## Testing

- **Approach:** API 통합 테스트 + UI 렌더링 수동 확인.
- **Key scenarios:** 목록/상세 조회, 타인 소유 접근 거부, 노트 없음 404.
- **Per-AC verification:** AC#1 → 목록 fixture 테스트; AC#2 → 상세 필드 확인; AC#3 → 타인 소유
  403 / 없는 노트 404 테스트; AC#4 → 수동으로 페이지 렌더 확인.
- **Fixtures / mocks:** 테스트 Note fixture(본인/타인).
- **Edge cases to cover:** `rawMarkdown`이 매우 긴 경우의 렌더링(스크롤 처리 수준으로 충분,
  별도 페이지네이션 불필요).

## Dependency Maps

- **Blocked by:** 4.1
- **Blocks:** 4.3, 5.2
- **External:** 없음

## Owned File/Module Scope

- `lib/domain/note/note-service.ts` (4.1과 공유 — Shared/contended, Blocked-by 4.1로 직렬화)
- `app/api/notes/route.ts`
- `app/api/notes/[noteId]/route.ts` (GET 부분 — 4.3이 DELETE 추가, Shared/contended)
- `app/(web)/notes/page.tsx`
- `app/(web)/notes/[noteId]/page.tsx`
- `tests/api/notes/list.test.ts`
- `tests/api/notes/detail.test.ts`

## Learnings from Previous Stories

- 4.1의 `note-service.ts` 구조를 재사용한다 (from 4.1).

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
