# 1.1: Next.js 프로젝트 초기화 및 Vercel 배포 연결

**Story ID:** 1.1
**Epic:** 1 — Foundation & Project Setup
**Slug:** nextjs-project-init
**Status:** done
**Branch:** `feature/1.1-nextjs-project-init` (base: `feature/nextjs-migration` — 실제 구현 시
develop 대신 feature/nextjs-migration을 base로 사용함. bmad-output/CLAUDE.md는 develop에
없고, develop은 이번 재구축과 무관한 레거시 Java/Vite 모노레포이기 때문. PR #9로
feature/nextjs-migration에 merge 완료)

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **개발자(본인)**,
I want **Next.js(App Router) 프로젝트를 초기화하고 Vercel에 배포 파이프라인을 연결하**,
so that **이후 모든 기능 개발을 이 위에서 진행하고, 매 변경사항을 즉시 배포·확인할 수 있다**.

## Acceptance Criteria

1. `create-next-app`으로 App Router 기반 TypeScript 프로젝트가 생성되어 있고, 로컬에서 `next dev`로 정상 구동된다.
2. 프로젝트가 Git 저장소에 커밋되어 있고 Vercel 프로젝트와 연결되어 있다.
3. main 브랜치 push 시 Vercel Production 배포가 자동으로 트리거된다.
4. 배포된 프로덕션 URL의 루트 페이지가 200 응답으로 접근 가능하다.
5. 프로젝트에 필요한 환경변수 자리표시자가 `.env.example`에 정의되어 있다 (`DATABASE_URL`,
   `AUTH_SECRET`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, `OPENAI_API_KEY`).

## Tasks / Subtasks

- [x] Next.js App Router + TypeScript 프로젝트 생성 (AC: #1)
  - [x] ESLint 기본 설정 확인 (AC: #1)
- [x] Git 저장소 초기화 및 최초 커밋 (AC: #2)
- [x] Vercel 프로젝트 생성 및 리포지토리 연결 (AC: #2)
- [x] Production 배포 트리거 확인 (main push) (AC: #3)
- [x] 배포된 루트 페이지 200 응답 확인 (AC: #4)
- [x] `.env.example` 작성 (AC: #5)

## Dev Notes

- 아키텍처는 Next.js 단일 풀스택(프런트+백엔드 통합)으로 확정되어 있다 — 별도 백엔드 프로젝트를
  만들지 않는다. [Source: architecture.md#adr-001-nextjs-단일-풀스택-애플리케이션]
- 컴포넌트 구조는 `app/(web)/**`(UI), `app/api/**`(Route Handler), `lib/domain/**`(도메인 로직),
  `lib/db`, `lib/auth.ts`로 나뉜다. 이번 스토리는 골격만 만들고 세부 폴더는 이후 스토리에서
  채운다. [Source: architecture.md#4-component-design]
- 비동기 분석 Job 처리를 위해 이후(Epic 3) Vercel Fluid Compute를 활성화해야 하므로, Vercel
  프로젝트 설정에서 Fluid Compute 옵션이 존재하는지 미리 확인해두면 좋다. [Source: architecture.md#adr-007-vercel-fluid-compute-기반-비동기-job-처리]
- 배포 전략은 Vercel Git 연동 자동 배포(main → Production, PR → Preview)이다. [Source: architecture.md#10-deployment-architecture]
- 환경변수 5종(DATABASE_URL/AUTH_SECRET/AUTH_GITHUB_ID/AUTH_GITHUB_SECRET/OPENAI_API_KEY)은
  각각 1.2(Prisma/Neon), 1.3(Auth.js), 4.1·5.1(OpenAI)에서 사용될 예정이므로 이번 스토리에서는
  값 없이 키 이름만 `.env.example`에 정의한다. [Inference]

## Testing

- **Approach:** 수동 검증. 이 스토리는 인프라 셋업이라 단위/통합 테스트 대상 로직이 없다.
- **Key scenarios:** 로컬 dev 서버 기동, Vercel 배포 성공, 루트 페이지 200 응답.
- **Per-AC verification:** AC#1 → `npm run dev` 로컬 구동 확인; AC#2 → Vercel 대시보드에서 프로젝트-리포지토리 연결 확인; AC#3 → git push 후 Vercel 배포 로그에서 자동 트리거 확인; AC#4 → 배포 URL에 `curl -I`로 200 확인; AC#5 → `.env.example` 파일에 5개 키 존재 확인.
- **Fixtures / mocks:** 없음.
- **Edge cases to cover:** 빌드 실패 시 Vercel 배포 로그 확인 절차(문서화 수준으로 충분).

## Dependency Maps

- **Blocked by:** none (최초 스토리)
- **Blocks:** 1.2, 1.3, 1.4 — 이후 모든 스토리가 이 프로젝트 골격 위에서 진행됨
- **External:** Vercel 계정, GitHub 저장소

## Owned File/Module Scope

- `package.json`
- `tsconfig.json`
- `next.config.ts`
- `eslint.config.mjs`
- `.env.example`
- `.gitignore`
- `app/layout.tsx`
- `app/page.tsx`
- `vercel.json` (필요 시)
- `package.json` (shared/contended — 이후 스토리들이 의존성을 추가할 때 이 파일을 함께 건드릴
  수 있음. 각 스토리는 Dependency Maps의 Blocked-by 체인으로 직렬화되어 있어 충돌하지 않는다)

## Learnings from Previous Stories

- (첫 스토리 — 이전 학습 없음)

## Dev Agent Record

**완료일:** 2026-08-03
**PR:** [#9](https://github.com/lsj0416/DevNote/pull/9) — `feature/1.1-nextjs-project-init` → `feature/nextjs-migration` (merged)

**진행 요약:**
- `create-next-app`(App Router, TypeScript, ESLint, no src-dir)으로 임시 디렉토리에 생성 후
  저장소 루트에 필요한 파일만 병합 (`package.json`, `tsconfig.json`, `next.config.ts`,
  `eslint.config.mjs`, `app/layout.tsx`, `app/page.tsx` 등). 기존 `README.md`, `docs/`,
  `bmad-output/`, `CLAUDE.md`는 그대로 유지.
- `npm run dev` / `npm run build` / `npm run lint` 모두 로컬에서 정상 통과 확인.
- 레거시 Spring Boot용 `.env.example`을 재구축 스펙(`DATABASE_URL`/`AUTH_SECRET`/
  `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET`/`OPENAI_API_KEY`)으로 교체.
- 저장소 루트에 untracked 상태로 남아있던 레거시 `backend/`, `frontend/` 디렉토리 삭제
  (develop 브랜치에 동일 내용이 커밋되어 있어 손실 없음). 이 디렉토리 때문에
  `npm run lint`가 `frontend/dist`의 번들 파일을 스캔해 1000+ 경고가 발생했었음 — 삭제로 해결.
- Vercel 프로젝트(`dev-note`)를 GitHub 저장소와 연결. Production Branch를 초기에는
  잘못 인지해 `develop`으로 시도했다가, `develop`이 레거시 브랜치임을 확인하고
  `feature/nextjs-migration`으로 Branch Tracking 설정.
- 최초 배포 시 Vercel Framework Preset이 Next.js로 설정되어 있지 않아
  (레거시 Vite 연결 잔재로 추정) 루트 페이지가 플랫폼 레벨 404를 반환하는 문제 발생 —
  Framework Preset을 Next.js로 재설정 후 재배포하여 해결.
- 최종 확인: `https://dev-note-kappa.vercel.app/` → `HTTP 200`, `x-nextjs-prerender: 1` 헤더 확인.

**후속 스토리(1.2)를 위한 참고:**
- Vercel 프로젝트의 Production Branch는 `feature/nextjs-migration`. 이후 스토리들도 이
  브랜치를 base/target으로 PR을 올리면 Preview 배포가 자동 생성됨.
- Vercel 프로젝트 Settings에서 Framework Preset/Output Directory override 여부를 항상
  한 번씩 확인할 것 (레거시 프로젝트 재사용 시 잘못된 값이 남아있을 수 있음).
