# 2.2: 내 정보 조회/수정 API + 프로필 화면

**Story ID:** 2.2
**Epic:** 2 — 인증 & 사용자 관리
**Slug:** user-profile-api-ui
**Status:** ready-for-dev
**Branch:** `feature/2.2-user-profile-api-ui` (base: `feature/nextjs-migration`)

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **사용자(본인)**,
I want **내 프로필을 조회하고 username을 수정하**,
so that **내 계정 정보를 원하는 대로 관리할 수 있다**.

## Acceptance Criteria

1. `GET /api/users/me`는 로그인된 사용자의 프로필(username, email, profileImage, createdAt)을
   반환한다.
2. `PATCH /api/users/me`는 username을 수정할 수 있고, 성공 시 갱신된 프로필을 반환한다.
3. `/profile` 페이지는 현재 사용자 프로필을 보여주고 username을 수정할 수 있는 폼을 제공한다.
4. 미인증 요청은 401 UNAUTHORIZED로 거부된다.
5. username이 빈 문자열이면 400 INVALID_REQUEST가 반환된다.

## Tasks / Subtasks

- [ ] `lib/domain/user/user-service.ts`에 `getProfile`/`updateUsername` 함수 작성 (AC: #1, #2)
- [ ] `GET /api/users/me` Route Handler (AC: #1, #4)
- [ ] `PATCH /api/users/me` Route Handler + 입력 검증 (AC: #2, #4, #5)
- [ ] `/profile` 페이지 + 수정 폼 UI (AC: #3)

## Dev Notes

- 1.4의 `requireUser()`/`ok()`/`fail()` 응답 헬퍼를 재사용한다 (ADR-005 LOCKED).
  [Source: architecture.md#adr-005-rest--json-응답-엔벨로프-규약]
- User 데이터는 1.2의 Prisma 스키마(username, email, profileImage 등)를 그대로 사용한다.
  [Source: architecture.md#5-data-model]
- FR-004는 Should 우선순위 요구사항이다. [Source: prd.md#fr-004-내-정보-조회수정--should]
- username 길이 제한은 PRD/architecture에 명시되어 있지 않아 합리적 상한(100자)을 적용한다.
  [Inference]

## Testing

- **Approach:** API 단위/통합 테스트 + UI 폼 제출 수동 확인.
- **Key scenarios:** 프로필 조회 성공, username 수정 성공, 미인증 401, 빈 username 400.
- **Per-AC verification:** AC#1 → 통합 테스트로 GET 응답 필드 확인; AC#2 → PATCH 후 DB 값 확인;
  AC#3 → 수동으로 폼 제출 후 화면 갱신 확인; AC#4 → 세션 없이 요청 시 401 확인; AC#5 → 빈 문자열
  PATCH 요청 시 400 확인.
- **Fixtures / mocks:** 로그인 세션 mock, 테스트 User 레코드.
- **Edge cases to cover:** 매우 긴 username(100자 초과) 입력 시 400 처리.

## Dependency Maps

- **Blocked by:** 1.4, 2.1 (레이아웃/가드)
- **Blocks:** 2.3 (프로필 페이지 재사용)
- **External:** 없음

## Owned File/Module Scope

- `lib/domain/user/user-service.ts`
- `app/api/users/me/route.ts`
- `app/(web)/profile/page.tsx`
- `components/user/ProfileForm.tsx`
- `tests/domain/user/user-service.test.ts`
- `tests/api/users/me.test.ts`

`lib/domain/user/user-service.ts`, `app/api/users/me/route.ts`, `app/(web)/profile/page.tsx`는
2.3(회원 탈퇴)이 DELETE 로직 추가를 위해 다시 수정하는 shared/contended 파일이다. 2.3은 2.2에
Blocked-by로 직렬화되어 있어 충돌하지 않는다.

## Learnings from Previous Stories

- 2.1의 레이아웃/가드 패턴을 재사용한다 (from 2.1).
- 1.4의 응답 헬퍼를 재사용한다 (from 1.4).

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
